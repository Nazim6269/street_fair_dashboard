import { ReactNode } from "react";

// This component is no longer used - tabs are now inline in layout.tsx
export default function SettingsSidebar({ children }: { children?: ReactNode }) {
    return <>{children}</>;
}
